services:
  database:
    image: 'postgres:17.4-alpine3.20'
    ports:
      - "5432:5432"
    volumes:
      - "db:/var/lib/postgresql/data/"
    env_file:
      - .env

  backend:
    build:
      context: .
      args:
        VERSION: 1.0
    ports:
      - "8080:8080"

volumes:
  db: {}